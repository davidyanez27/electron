document.getElementById("autentica").addEventListener('click', function(event) {
    // Prevent the default action (navigation) if needed
    event.preventDefault();
    function iframe() {
        console.log(document.getElementById("myIframe").src);
    }
    
});




